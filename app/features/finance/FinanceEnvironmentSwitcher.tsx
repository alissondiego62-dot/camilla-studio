"use client";
import type { FinanceEnvironment } from "./types";
export function FinanceEnvironmentSwitcher({value,onChange,personal,professional,consolidated}:{value:FinanceEnvironment;onChange:(value:FinanceEnvironment)=>void;personal:boolean;professional:boolean;consolidated:boolean}){return <div className="cs-finance-environments" role="tablist" aria-label="Ambiente financeiro">{professional&&<button type="button" className={value==="professional"?"active":""} onClick={()=>onChange("professional")}>Profissional / CNPJ</button>}{personal&&<button type="button" className={value==="personal"?"active":""} onClick={()=>onChange("personal")}>Pessoal</button>}{consolidated&&<button type="button" className={value==="consolidated"?"active":""} onClick={()=>onChange("consolidated")}>Consolidado</button>}</div>}
