"use client";
import { Button } from "@/app/components/ui/Button";
import { FinanceFilters } from "./FinanceFilters";
import type { FinanceFilters as Filters, FinancePeriodPreset, FinanceWorkspaceOptions } from "./types";
export function FinanceToolbar({filters,onFilters,onPeriod,options,environment,onNew,onTransfer,onRecurrence,onReload,canCreate,canTransfer,canRecurrence}:{filters:Filters;onFilters:(value:Filters)=>void;onPeriod:(period:FinancePeriodPreset)=>void;options:FinanceWorkspaceOptions;environment:string;onNew:()=>void;onTransfer:()=>void;onRecurrence:()=>void;onReload:()=>void;canCreate:boolean;canTransfer:boolean;canRecurrence:boolean}){return <div className="cs-finance-toolbar"><div className="cs-finance-periods">{(["today","week","month","quarter","year","custom"] as FinancePeriodPreset[]).map((period)=><button type="button" key={period} className={filters.period===period?"active":""} onClick={()=>onPeriod(period)}>{{today:"Hoje",week:"Semana",month:"Mês",quarter:"Trimestre",year:"Ano",custom:"Personalizado"}[period]}</button>)}</div><FinanceFilters filters={filters} onChange={onFilters} options={options} environment={environment}/><div className="cs-finance-toolbar-actions">{canTransfer&&<Button onClick={onTransfer}>Transferência</Button>}{canRecurrence&&<Button onClick={onRecurrence}>Processar recorrências</Button>}<Button onClick={onReload}>Atualizar</Button>{canCreate&&<Button variant="primary" onClick={onNew}>Novo lançamento</Button>}</div></div>}
