"use client";
import { Button } from "@/app/components/ui/Button";
import { dateOnly } from "@/app/config/regions";
import { formatMoney } from "../finance.money";
import type { FinanceEntryRow } from "../types";
const labels:Record<string,string>={forecast:"Previsto",pending:"Pendente",paid:"Pago",received:"Recebido",partially_paid:"Parcialmente pago",partially_received:"Parcialmente recebido",overdue:"Vencido",cancelled:"Cancelado",under_review:"Em análise",awaiting_approval:"Aguardando aprovação"};
export function FinanceEntriesTable({items,canViewValues,canSettle,onOpen,onSettle}:{items:FinanceEntryRow[];canViewValues:boolean;canSettle:boolean;onOpen:(id:string)=>void;onSettle:(entry:FinanceEntryRow)=>void}){return <div className="cs-table-wrap"><table className="cs-table cs-finance-table"><thead><tr><th>Descrição</th><th>Competência</th><th>Vencimento</th><th>Categoria</th><th>Conta</th><th>Status</th><th>Total</th><th>Em aberto</th><th>Ações</th></tr></thead><tbody>{items.map((item)=><tr key={item.id} className={item.archived_at?"is-archived":""}><td data-label="Descrição"><button type="button" className="cs-finance-entry-link" onClick={()=>onOpen(item.id)}><strong>{item.description}</strong><small>{item.client_name||item.supplier_name||item.project_name||item.document_number||"Sem vínculo"}</small></button></td><td data-label="Competência">{dateOnly(item.competence_date)}</td><td data-label="Vencimento" className={item.effective_status==="overdue"?"is-overdue":""}>{dateOnly(item.due_date)}</td><td data-label="Categoria">{item.category_name||"—"}</td><td data-label="Conta">{item.account_name||item.card_name||"—"}</td><td data-label="Status"><span className={`cs-finance-status status-${item.effective_status}`}>{labels[item.effective_status]??item.effective_status}</span></td><td data-label="Total"><strong>{formatMoney(item.amount,canViewValues)}</strong></td><td data-label="Em aberto">{formatMoney(item.open_amount,canViewValues)}</td><td data-label="Ações"><div className="cs-finance-row-actions"><Button variant="text" onClick={()=>onOpen(item.id)}>Abrir</Button>{canSettle&&item.open_amount!=="0.00"&&item.status!=="cancelled"&&<Button variant="text" onClick={()=>onSettle(item)}>Baixar</Button>}</div></td></tr>)}</tbody></table></div>}
