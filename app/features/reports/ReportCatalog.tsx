import type { ReportDefinition } from "./types";
export function ReportCatalog({items,selected,onSelect}:{items:ReportDefinition[];selected:string;onSelect:(code:ReportDefinition["code"])=>void}){const groups=[...new Set(items.map((item)=>item.group))];return <nav className="cs-report-catalog">{groups.map((group)=><section key={group}><h3>{group}</h3>{items.filter((item)=>item.group===group).map((item)=><button key={item.code} type="button" className={selected===item.code?"active":""} onClick={()=>onSelect(item.code)}><strong>{item.title}</strong><small>{item.description}</small></button>)}</section>)}</nav>}
