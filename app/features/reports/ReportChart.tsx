export function ReportChart({items}:{items:Array<{label:string;value:number;color?:string|null}>}){const max=Math.max(1,...items.map((item)=>Number(item.value)));return <div className="cs-report-chart">{items.length===0?<p className="cs-empty-note">Este relatório não possui série gráfica.</p>:items.map((item,index)=><article key={`${item.label}-${index}`}><header><span>{item.label}</span><strong>{item.value}</strong></header><div><i style={{width:`${Math.max(2,(item.value/max)*100)}%`,background:item.color||`var(--cs-chart-${index%6+1})`}}/></div></article>)}</div>}
