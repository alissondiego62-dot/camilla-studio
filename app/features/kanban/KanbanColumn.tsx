"use client";
import { ProjectKanbanCard } from "./ProjectKanbanCard";
import type { AssignableUser, KanbanProject, WorkflowOption, WorkflowPatch } from "./types";
export function KanbanColumn({stage,stageName,stages,statuses,projects,users,canStage,canStatus,canResponsible,pendingIds,activeDrop,onDrop,onDragEnter,onPatch,onDragStart,onDragEnd}:{stage:string;stageName:string;stages:WorkflowOption[];statuses:WorkflowOption[];projects:KanbanProject[];users:AssignableUser[];canStage:boolean;canStatus:boolean;canResponsible:boolean;pendingIds:Set<string>;activeDrop:boolean;onDrop:(projectId:string,stage:string)=>void;onDragEnter:(stage:string|null)=>void;onPatch:(project:KanbanProject,patch:WorkflowPatch)=>void;onDragStart:(id:string)=>void;onDragEnd:()=>void}){
return <section className={`cs-kanban-column ${activeDrop?"is-drop-target":""}`} onDragOver={e=>{if(canStage){e.preventDefault();e.dataTransfer.dropEffect="move"}}} onDragEnter={()=>canStage&&onDragEnter(stage)} onDragLeave={e=>{if(!e.currentTarget.contains(e.relatedTarget as Node|null))onDragEnter(null)}} onDrop={e=>{e.preventDefault();const id=e.dataTransfer.getData("text/plain");if(id)onDrop(id,stage);onDragEnter(null)}}><header className="cs-kanban-column-header"><h2>{stageName}</h2><span>{projects.length}</span></header><div className="cs-kanban-lane">{projects.length===0?<p className="cs-kanban-empty">Nenhum projeto</p>:projects.map(project=><ProjectKanbanCard key={project.id} project={project} stages={stages} statuses={statuses} users={users} canStage={canStage} canStatus={canStatus} canResponsible={canResponsible} pending={pendingIds.has(project.id)} onPatch={patch=>onPatch(project,patch)} onDragStart={()=>onDragStart(project.id)} onDragEnd={onDragEnd}/>)}</div></section>}
