import { CAMILLA_BRAND } from "@/app/config/brand";import{CAMILLA_LOCALE,CAMILLA_TIME_ZONE,CAMILLA_CURRENCY}from"@/app/config/regions";import{ModuleFrame}from"@/app/components/ui/ModuleFrame";export function SettingsPage(){const cards=[["Identidade",`${CAMILLA_BRAND.companyName} · logo em ${CAMILLA_BRAND.logoPath}`],["Paleta",Object.values(CAMILLA_BRAND.colors).map(c=>c.hex).join(" · ")],["Idioma",CAMILLA_LOCALE],["Fuso horário",CAMILLA_TIME_ZONE],["Moeda",`${CAMILLA_CURRENCY} · R$`],["Versão","Camilla Studio 3.0 — Etapa 01"]];return <ModuleFrame title="Configurações" subtitle="Informações técnicas e regionais ativas"><section className="cs-settings-grid">{cards.map(([title,description])=><article className="cs-card" key={title}><h2>{title}</h2><p>{description}</p></article>)}</section></ModuleFrame>}
