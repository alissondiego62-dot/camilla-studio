"use client";
import{useCallback}from"react";import{ModuleFrame}from"@/app/components/ui/ModuleFrame";import{Button}from"@/app/components/ui/Button";import{EmptyState,ErrorState,LoadingState}from"@/app/components/ui/DataState";import{useModuleData}from"@/app/hooks/useModuleData";import{usePermissions}from"@/app/hooks/usePermissions";import{AccessDenied}from"@/app/components/security/PermissionGate";import{dateTime}from"@/app/config/regions";import{listAudits}from"./settings.service";
export function SecuritySettingsPage(){const{can}=usePermissions();const loader=useCallback(()=>listAudits(150),[]);const{data,loading,error,reload}=useModuleData(loader,[]);if(!can("security","view"))return <ModuleFrame title="Segurança"><AccessDenied/></ModuleFrame>;return <ModuleFrame title="Segurança e auditoria" subtitle="Eventos críticos sem senhas, tokens ou credenciais" actions={<Button onClick={()=>void reload()}>Atualizar</Button>}>{error&&<ErrorState message={error} onRetry={()=>void reload()}/>}<section className="cs-card"><h2>Proteções ativas no SQL da Etapa 02</h2><ul className="cs-clean-list"><li>RLS por perfil, ação e escopo.</li><li>Bloqueio de usuário e revogação de sessões.</li><li>Proteção transacional do último administrador.</li><li>Exclusão lógica de registros vinculados.</li><li>Auditoria de login e mudanças administrativas.</li></ul></section>{loading?<LoadingState/>:data.length===0?<EmptyState title="Nenhum evento registrado" description="Os eventos aparecerão após aplicar o SQL e utilizar as funções administrativas."/>:<div className="cs-table-wrap"><table className="cs-table"><thead><tr><th>Data</th><th>Evento</th><th>Alvo</th><th>Metadados</th></tr></thead><tbody>{data.map(row=><tr key={String(row.id)}><td data-label="Data">{dateTime(row.created_at)}</td><td data-label="Evento"><strong>{row.event_type}</strong></td><td data-label="Alvo">{row.target_type??"—"}{row.target_id?` · ${row.target_id}`:""}</td><td data-label="Metadados"><code>{JSON.stringify(row.metadata)}</code></td></tr>)}</tbody></table></div>}</ModuleFrame>}
