"use client";
import{useCallback,useState}from"react";import type{FormEvent}from"react";import{ModuleFrame}from"@/app/components/ui/ModuleFrame";import{Button}from"@/app/components/ui/Button";import{Modal}from"@/app/components/ui/Modal";import{FeedbackMessage}from"@/app/components/ui/FeedbackMessage";import{EmptyState,ErrorState,LoadingState}from"@/app/components/ui/DataState";import{FormField}from"@/app/components/ui/FormField";import{useModuleData}from"@/app/hooks/useModuleData";import{useAsyncAction}from"@/app/hooks/useAsyncAction";import{usePermissions}from"@/app/hooks/usePermissions";import type{WorkflowKind,WorkflowRow}from"./types";import{listWorkflow,saveWorkflow}from"./settings.service";
const labels:Record<WorkflowKind,string>={project_stages:"Etapas dos projetos",project_statuses:"Status dos projetos",activity_statuses:"Status das atividades"};
export function WorkflowSettingsPage(){const{can}=usePermissions();const[kind,setKind]=useState<WorkflowKind>("project_stages");const loader=useCallback(()=>listWorkflow(kind),[kind]);const{data,loading,error,reload}=useModuleData(loader,[]);const[editing,setEditing]=useState<WorkflowRow|undefined>();const[open,setOpen]=useState(false);const{pending,error:actionError,success,run}=useAsyncAction();async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget);const result=await run(()=>saveWorkflow(kind,{id:editing?.id,code:String(f.get("code")),name:String(f.get("name")),color:String(f.get("color")||""),position:Number(f.get("position")||0),active:f.get("active")==="on",final:f.get("final")==="on"}),"Catálogo atualizado.");if(result.ok){setOpen(false);void reload()}}return <ModuleFrame title="Etapas e status" subtitle="Catálogos preservam códigos usados nos registros" actions={can("settings","manage_settings")?<Button variant="primary" onClick={()=>{setEditing(undefined);setOpen(true)}}>Adicionar</Button>:undefined}><div className="cs-segmented">{(Object.keys(labels)as WorkflowKind[]).map(item=><button type="button" className={kind===item?"active":""} key={item} onClick={()=>setKind(item)}>{labels[item]}</button>)}</div><FeedbackMessage error={actionError} success={success}/>{error&&<ErrorState message={error} onRetry={()=>void reload()}/>} {loading?<LoadingState/>:data.length===0?<EmptyState title="Catálogo ainda não aplicado" description="Aplique o SQL da Etapa 02 para carregar os valores atuais."/>:<div className="cs-table-wrap"><table className="cs-table"><thead><tr><th>Ordem</th><th>Nome</th><th>Código</th><th>Estado</th><th>Final</th><th>Ações</th></tr></thead><tbody>{data.map(row=><tr key={row.id}><td data-label="Ordem">{row.position}</td><td data-label="Nome"><strong>{row.name}</strong></td><td data-label="Código">{row.code}</td><td data-label="Estado">{row.active?"Ativo":"Inativo"}</td><td data-label="Final">{row.final?"Sim":"Não"}</td><td data-label="Ações">{can("settings","manage_settings")&&<Button variant="text" onClick={()=>{setEditing(row);setOpen(true)}}>Editar</Button>}</td></tr>)}</tbody></table></div>}{open&&<Modal title={editing?"Editar item":"Adicionar item"} onClose={()=>setOpen(false)}><form className="cs-form-grid" onSubmit={submit}><FormField label="Nome" name="name" required defaultValue={editing?.name}/><FormField label="Código" name="code" required defaultValue={editing?.code}/><FormField label="Cor" name="color" type="color" defaultValue={editing?.color??"#9b6352"}/><FormField label="Posição" name="position" type="number" min="0" defaultValue={editing?.position??data.length}/><label className="cs-check-option"><input type="checkbox" name="active" defaultChecked={editing?.active??true}/><span>Ativo</span></label><label className="cs-check-option"><input type="checkbox" name="final" defaultChecked={editing?.final??false}/><span>Estado final</span></label><div className="cs-span-2"><FeedbackMessage error={actionError}/></div><div className="cs-form-actions"><Button type="button" onClick={()=>setOpen(false)}>Cancelar</Button><Button variant="primary" loading={pending}>Salvar</Button></div></form></Modal>}</ModuleFrame>}
