"use client";
import { Button } from "@/app/components/ui/Button";
import type { ClientFilters, ClientFormOptions } from "./types";
export function ClientsFilters({value,options,onChange,onApply,onClear}:{value:ClientFilters;options:ClientFormOptions;onChange:(value:ClientFilters)=>void;onApply:()=>void;onClear:()=>void}){const set=(key:keyof ClientFilters,next:string|boolean)=>onChange({...value,[key]:next});return <div className="cs-client-filters"><input aria-label="Pesquisar clientes" placeholder="Nome, CPF, CNPJ, telefone, WhatsApp ou e-mail" value={value.query} onChange={e=>set("query",e.target.value)} onKeyDown={e=>{if(e.key==="Enter")onApply()}}/><select aria-label="Tipo de pessoa" value={value.personType} onChange={e=>set("personType",e.target.value)}><option value="">Todos os tipos</option><option value="person">Pessoa física</option><option value="company">Pessoa jurídica</option></select><select aria-label="Status do relacionamento" value={value.relationshipStatus} onChange={e=>set("relationshipStatus",e.target.value)}><option value="">Todos os status</option>{options.relationshipStatuses.map(item=><option key={item.code} value={item.code}>{item.name}</option>)}</select><select aria-label="Responsável interno" value={value.responsibleId} onChange={e=>set("responsibleId",e.target.value)}><option value="">Todos os responsáveis</option>{options.users.map(user=><option key={user.id} value={user.id}>{user.name}</option>)}</select><label className="cs-inline-check"><input type="checkbox" checked={value.includeArchived} onChange={e=>set("includeArchived",e.target.checked)}/><span>Arquivados</span></label><Button variant="primary" onClick={onApply}>Aplicar</Button><Button variant="text" onClick={onClear}>Limpar</Button></div>}
