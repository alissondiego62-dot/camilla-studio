"use client";
import type { FormEvent } from "react";
import { Button } from "@/app/components/ui/Button";
import { dateTime } from "@/app/config/regions";
import type { ClientCategory, ClientNote } from "./types";
export function ClientNoteForm({note,types,pending,onSubmit,onCancel}:{note?:ClientNote|null;types:ClientCategory[];pending:boolean;onSubmit:(value:{id?:string;note_type:string;content:string;important:boolean;occurred_at:string})=>void;onCancel:()=>void}){function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget);onSubmit({id:note?.id,note_type:String(f.get("note_type")||"general"),content:String(f.get("content")||"").trim(),important:f.get("important")==="on",occurred_at:new Date(String(f.get("occurred_at"))).toISOString()})}const local=(value:string)=>{const d=new Date(value);return new Date(d.getTime()-d.getTimezoneOffset()*60000).toISOString().slice(0,16)};return <form className="cs-form-grid" onSubmit={submit}><label><span>Tipo</span><select name="note_type" defaultValue={note?.note_type??"general"}>{(types.length?types:[{code:"general",name:"Observação geral",color:null},{code:"contact",name:"Contato",color:null}]).map(item=><option key={item.code} value={item.code}>{item.name}</option>)}</select></label><label><span>Data e horário</span><input name="occurred_at" type="datetime-local" required defaultValue={local(note?.occurred_at??new Date().toISOString())}/></label><label className="cs-span-2"><span>Observação</span><textarea name="content" rows={6} required defaultValue={note?.content??""}/></label><label className="cs-check-option"><input name="important" type="checkbox" defaultChecked={note?.important??false}/><span>Destacar como importante</span></label>{note&&<small className="cs-span-2">Última atualização: {dateTime(note.updated_at)}</small>}<div className="cs-form-actions"><Button type="button" onClick={onCancel}>Cancelar</Button><Button variant="primary" loading={pending}>Salvar observação</Button></div></form>}
