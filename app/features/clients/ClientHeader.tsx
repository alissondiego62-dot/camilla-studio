"use client";
import Link from "next/link";
import { Button } from "@/app/components/ui/Button";
import type { ClientRow } from "./types";
export function ClientHeader({client,canEdit,canArchive,onEdit,onArchive,onReactivate}:{client:ClientRow;canEdit:boolean;canArchive:boolean;onEdit:()=>void;onArchive:()=>void;onReactivate:()=>void}){return <header className="cs-client-detail-header"><div><Link href="/clients" className="cs-back-link">← Clientes</Link><span className="cs-kicker">{client.person_type==="company"?"Pessoa jurídica":"Pessoa física"}</span><h1>{client.trade_name||client.name}</h1><p>{client.legal_name&&client.legal_name!==client.name?client.legal_name:"Ficha completa do cliente"}</p><div className="cs-client-header-meta"><span className="cs-badge">{client.relationship_status}</span>{client.internal_responsible?.name&&<span>Responsável: {client.internal_responsible.name}</span>}{client.archived_at&&<span className="cs-badge cs-badge-danger">Arquivado</span>}</div></div><div className="cs-page-actions">{canEdit&&<Button onClick={onEdit}>Editar cadastro</Button>}{canArchive&&(client.archived_at?<Button variant="primary" onClick={onReactivate}>Reativar</Button>:<Button variant="danger" onClick={onArchive}>Arquivar</Button>)}</div></header>}
