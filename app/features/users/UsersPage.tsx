"use client";
import{useCallback}from"react";import{dateTime}from"@/app/config/regions";import{ModuleFrame}from"@/app/components/ui/ModuleFrame";import{Button}from"@/app/components/ui/Button";import{EmptyState,ErrorState,LoadingState}from"@/app/components/ui/DataState";import{useModuleData}from"@/app/hooks/useModuleData";import{listProfiles}from"./users.service";export function UsersPage(){const loader=useCallback(()=>listProfiles(),[]);const{data:items,loading,error,reload}=useModuleData(loader,[]);return <ModuleFrame title="Usuários" subtitle="Perfis e acesso ao sistema" actions={<Button onClick={()=>void reload()}>Atualizar</Button>}>{error&&<ErrorState message={error} onRetry={()=>void reload()}/>} {loading?<LoadingState/>:items.length===0?<EmptyState title="Nenhum perfil" description="Os usuários autenticados aparecerão aqui."/>:<div className="cs-table-wrap"><table className="cs-table"><thead><tr><th>Usuário</th><th>Perfil</th><th>Status</th><th>Último acesso</th></tr></thead><tbody>{items.map(u=><tr key={u.id}><td data-label="Usuário"><strong>{u.name}</strong><span>{u.email}</span></td><td data-label="Perfil">{u.camilla_role??u.role??"viewer"}</td><td data-label="Status">{u.active?"Ativo":"Inativo"}</td><td data-label="Último acesso">{dateTime(u.last_access_at)}</td></tr>)}</tbody></table></div>}</ModuleFrame>}
