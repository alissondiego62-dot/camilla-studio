import { dateTime } from "@/app/config/regions";
import type { DriveConnectionStatus } from "./types";
export function DriveConnectionCard({status}:{status:DriveConnectionStatus}){return <article className="cs-card cs-drive-connection-card"><header><div><h2>Google Drive</h2><p>Suporte de arquivos; o banco continua sendo a fonte das relações e permissões.</p></div><span className={`cs-badge ${status.connected?"success":"warning"}`}>{status.connected?"Conectado":status.configured?"Desconectado":"Configuração necessária"}</span></header><dl className="cs-summary-list"><div><dt>Conta</dt><dd>{status.google_account_email??"Nenhuma conta conectada"}</dd></div><div><dt>Pasta raiz</dt><dd>{status.root_folder_name}</dd></div><div><dt>Situação</dt><dd>{status.connection_status??"Não iniciada"}</dd></div><div><dt>Último teste</dt><dd>{dateTime(status.last_checked_at)}</dd></div></dl>{status.last_error&&<p className="cs-alert cs-alert-error">{status.last_error}</p>}</article>}
