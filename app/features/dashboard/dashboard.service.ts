import { supabase } from "@/lib/supabase"; import { ensureSupabase, assertNoError } from "@/app/services/supabase/base-service"; import type { DashboardStats } from "./types";
export async function loadDashboardStats():Promise<DashboardStats>{ if(!ensureSupabase())return{projects:0,late:0,activities:0,clients:0,income:0,expense:0}; const today=new Date().toISOString().slice(0,10); const [p,l,a,c,f]=await Promise.all([supabase.from("projects").select("id",{count:"exact",head:true}).neq("stage","completed"),supabase.from("projects").select("id",{count:"exact",head:true}).lt("main_deadline",today).neq("stage","completed"),supabase.from("project_activities").select("id",{count:"exact",head:true}).neq("status","completed"),supabase.from("clients").select("id",{count:"exact",head:true}),supabase.from("financial_entries").select("entry_type,amount")]); [p,l,a,c,f].forEach(assertNoError); let income=0,expense=0; for(const row of (f.data??[]) as Array<{entry_type:string;amount:number|string}>){if(row.entry_type==="income")income+=Number(row.amount);if(row.entry_type==="expense")expense+=Number(row.amount)} return{projects:p.count??0,late:l.count??0,activities:a.count??0,clients:c.count??0,income,expense}; }
